<table>
  <tr>
    <th>Email</th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td id="<%= user.id %>"><%= link_to user.role, admin_update_user_role_path(user.id), remote: true, method: :post %></td>
  </tr>
<% end %>

</table>
