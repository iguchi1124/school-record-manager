<ol class="breadcrumb">
  <%= render_breadcrumbs tag: 'li', separator: '' %>
</ol>

<ul>
  <% @schools.each do |school| %>
    <li class="list-item">
      <%= link_to school.name, registrations_school_path(school), class: 'btn btn-primary' %>
    </li>
  <% end %>
</ul>
