<div class="page-header">
  <h1>成績</h1>
</div>

<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <% @record_terms.each do |term| %>
      <%= link_to test_title(term), record_path(term), class: 'btn btn-primary btn-lg btn-block' %>
    <% end %>
  </div>
</div>
